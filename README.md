1. cài thư viện
pip install selenium
pip install webdriver-manager
pip install undetected-chromedriver

2. chạy file crawl.py để cào branch-model-category-title
(lưu ý thay thế TARGET_BRAND cần cào)
-> sau khi xong sẽ tạo file BRAND_Complete.json


thay thế input_file = file BRAND_Complete.json
3. chạy file crawl_part.py -> oke